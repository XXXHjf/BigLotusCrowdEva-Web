1. 因果链

以被选中结点为起点，将其视为一条因果传播路径中的“起因”结点，展示它可能进一步影响的后续结点。
该子图结构为单向链式结构，结点之间按时间或因果顺序依次连接，最多展示 3 层（即 3 个结点），用于体现影响的逐步传递过程。

2. 枢纽节点

以被选中结点为中心，展示其作为核心枢纽直接指向多个相关结点的关系。
该子图采用**单层有向星型结构**，仅包含**中心结点**及其**一层直接相连的外部结点**。中心结点向外发出多条有向边，外层结点不再继续向外扩展，用于体现该结点的集中辐射作用。

3. 社区结构

以被选中结点为核心，展示与其联系紧密、相互影响较强的一组结点。
该子图整体表现为一个**局部稠密的有向图**，强调多个结点之间存在**双向或多向影响关系**。从外形上看，它既可能是**强连通有向子图**，也可能表现为**首尾相接的有向环状结构**，或在环状基础上带有少量内部连接，用于体现结点之间的群体性交互特征。

4.关键结点

以被选中结点为中心，展示其作为连接不同区域的桥梁结点。
该子图采用**三层桥接型有向结构**，最多展示 **3 层**：**流入结点层—中心关键结点—流出结点层**。其中，流入层的结点指向中心结点，中心结点再指向流出层的结点，整体不再向外扩展，用于突出该结点的桥接和中转作用。